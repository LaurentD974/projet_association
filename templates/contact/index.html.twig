{% extends 'base.html.twig' %}



{% block title %}Contactez le Provincial{% endblock %}

{% block body %}
{% for label, messages in app.flashes %}
  {% for message in messages %}
    <div class="alert alert-{{ label == 'error' ? 'danger' : 'success' }}">
      {{ message }}
    </div>
  {% endfor %}
{% endfor %}
<header>
  <div class="header-left">
    <a href="https://www.compagnons-du-devoir.com/" target="_blank" rel="noopener">
      <img
        src="{{ asset('image/aocdtf-logo.svg') }}"
        alt="Logo des Compagnons du Devoir"
        class="logo"
      />
    </a>
  </div>
  <div class="header-center">
    <h1 class="site-title">
      Association Ouvrière des Compagnons du Devoir et du Tour de France<br />
      <span class="ile" aria-label="île">🏝️</span> Île de la Réunion
      <span class="palmier" aria-label="Palmier">🌴</span>
    </h1>
  </div>
 
</header>
<section class="contact-section">
  <div class="container">
    <h2>Contactez le Provincial <strong>📞 +262 693 62 62 82</strong></h2>
<p>Pour toute demande officielle, vous pouvez utiliser ce formulaire de contact. Nous vous répondrons dans les plus brefs délais.</p>

    <form method="POST" action="{{ path('contact_send') }}" class="contact-form">
      <div class="mb-1">
        <label for="nom" class="form-label">Votre nom</label>
        <input type="text" class="form-control" id="nom" name="nom" required>
      </div>
      <div class="mb-1">
        <label for="email" class="form-label">Votre adresse email</label>
        <input type="email" class="form-control" id="email" name="email" required>
      </div>
      <div class="mb-1">
        <label for="message" class="form-label">Votre message</label>
        <textarea class="form-control" id="message" name="message" rows="3" required></textarea>
      </div>
      <input type="hidden" name="_token" value="{{ csrf_token('contact_form') }}">
      <div class="form-buttons">
  <button type="submit" class="btn btn-primary">Envoyer</button>
  <a href="{{ path('home') }}" class="btn btn-secondary">Retour à l'accueil</a>
</div>
    </form>
  </div>
</section>
<style>
  .contact-section h2,
  .contact-section p {
    text-align: center;
  }
  .form-buttons {
  display: flex;
  justify-content: space-between;
  margin-top: 1rem;
}
</style>
{% endblock %}