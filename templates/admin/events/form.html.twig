{% extends 'base.html.twig' %}

{% block title %}
  {{ event is defined ? 'Modifier un événement' : 'Créer un événement' }}
{% endblock %}

{% block body %}
<div class="container py-4">
  <div class="d-flex align-items-center justify-content-between mb-3">
    <h1 class="h4 text-primary m-0">
      {{ event is defined ? '✏️ Modifier un événement' : '➕ Créer un événement' }}
    </h1>
    <a href="{{ path('admin_events_list') }}" class="btn btn-outline-secondary">⬅️ Retour à la liste</a>
  </div>

  <div class="card shadow border-0">
    <div class="card-body">
      {{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}
        {{ form_errors(form) }}

        <div class="mb-3">
          {{ form_row(form.title, {
            label: 'Titre',
            attr: { class: 'form-control', placeholder: 'Ex : Réunion de coordination' }
          }) }}
        </div>

        <div class="mb-3">
          {{ form_row(form.description, {
            label: 'Description',
            attr: { class: 'form-control', rows: 4, placeholder: 'Détails, ordre du jour…' }
          }) }}
        </div>

        <div class="mb-3">
          {{ form_row(form.location, {
            label: 'Lieu',
            attr: { class: 'form-control', placeholder: 'Ex : Siège, Salle 2' }
          }) }}
        </div>

        <div class="row">
          <div class="col-md-6 mb-3">
            {{ form_row(form.startDate, {
              label: 'Début',
              attr: { class: 'form-control' }
            }) }}
          </div>
          <div class="col-md-6 mb-3">
            {{ form_row(form.endDate, {
              label: 'Fin',
              attr: { class: 'form-control' }
            }) }}
          </div>
        </div>

        <div class="mb-3">
          {{ form_row(form.type, {
            label: 'Type d’événement',
            attr: { class: 'form-select' }
          }) }}
        </div>

        <div class="form-check mb-3">
          {{ form_widget(form.isValidated, { attr: { class: 'form-check-input' } }) }}
          {{ form_label(form.isValidated, 'Validé', { label_attr: { class: 'form-check-label' } }) }}
          {{ form_errors(form.isValidated) }}
        </div>

        <div class="d-flex gap-2">
          <button class="btn btn-primary" type="submit">💾 Enregistrer</button>
          <a href="{{ path('admin_events_list') }}" class="btn btn-outline-secondary">Annuler</a>
        </div>
      {{ form_end(form) }}
    </div>
  </div>
</div>
{% endblock %}
