<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Bienvenue!{% endblock %}</title>
<link rel="icon" type="image/png" href="{{ asset('image/favicon-aocdtf.png') }}">

    {# Désactivation temporaire de Turbo #}
    <script>
        window.Turbo = { session: { drive: false } };
    </script>

    {# Bootstrap CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

     {# Supprime les liens cassés ou remplace-les par ceux que tu utilises réellement #}
    <link rel="stylesheet" href="/build/app.css"> {# ton CSS compilé via Webpack Encore #}

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {# Bootstrap JS #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
    {% block body %}{% endblock %}
</body>
</html>